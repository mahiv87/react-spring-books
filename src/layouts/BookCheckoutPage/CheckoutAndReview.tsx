import BookModel from '../../models/BookModel';

const CheckoutAndReview: React.FC<{ book: BookModel | undefined }> = (
	props
) => {
	return (
		<div>
			<div>CheckoutAndReview</div>
		</div>
	);
};

export default CheckoutAndReview;
